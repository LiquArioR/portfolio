# Сетевой стенд: конфигурации и проверки

Ниже — выборка из выполненного учебного отчёта `DO2_LinuxNetwork.ID_356275-1/src/Report.md`. Стенд состоял из рабочих станций `ws11`, `ws21`, `ws22` и маршрутизаторов `r1`, `r2`. Сохранившиеся скриншоты финальных этапов размещены на внешнем хостинге из исходного отчёта.

## Статическая маршрутизация

На хостах настроены адреса и маршруты через netplan, на маршрутизаторах включена пересылка IP-пакетов. В исходном отчёте отмечены проверки `ping`, `ip r`, `tcpdump` и `traceroute` для поиска пути пакета через `r1` и `r2`.

## DHCP

На `r2` настроен DHCP-сервер; клиенты `ws21` и `ws22` получают адреса из диапазона `10.20.0.2–10.20.0.50`. На `r1` отдельно выполнена привязка адреса `ws11` к MAC-адресу. В отчёте зафиксированы параметры `range`, `option routers`, `option domain-name-servers`, `hardware ethernet` и `fixed-address`.

## Межсетевой экран и NAT

На `r2` применялись правила `iptables` для фильтрации транзитного трафика, затем SNAT и DNAT. В отчёте зафиксированы конфигурация скрипта и проверка HTTP-доступа к Apache через трансляцию адресов.

![Скрипт iptables с правилами NAT](https://s10.iimage.su/s/12/gjk2VGhxI4afTQKUPUg6wS3ThID2GJbwIztAKDAFF.png)

![Проверка доступа через SNAT](https://s10.iimage.su/s/12/gdkpbGWxlJnzPYSyGDYn577PT4wNX3j6lBzw2Xs7F.png)

![Проверка доступа через DNAT](https://s10.iimage.su/s/12/gNC8Y9BxAFgKAzPhyeBC1GkWsi2md1wrfUk7Zew0z.png)

## SSH-туннель

Локальный проброс порта `8080` на `ws21` к Apache, который слушает `localhost:80` на `ws22`; проверка выполнена обращением к локальному порту. В отчёте указан параметр `-L 8080:localhost:80`.

![Apache на ws22 слушает только localhost](https://s10.iimage.su/s/12/g1DR553xLlUzKs1mOYc3cnBJL445FT4jwGAUV4mpi.png)

Это фрагменты учебной лаборатории. Часть ссылок на старые скриншоты исходного отчёта больше не открывается, поэтому здесь приведены только доступные подтверждения. Конфигурация не предлагается как готовый шаблон для production-сети.
